package controllers

func CreateApi() {
	// split the yaml into infra and workload

	// set the desired state to a value. There will be a controller service that will do what is needed to bring the infra up to date

	// the default service will just be a hello world. This will need to pass a test before the next phase starts
	// then the model needs to be applied

	// put the yaml file into teh key-value store
	// configured to trigger a lambda when entered
}

func DeleteApi() {
	// schedule all infrastructure for deletion
}